import { SearchDealerCodePipe } from './search-dealer-code.pipe';

describe('SearchDealerCodePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchDealerCodePipe();
    expect(pipe).toBeTruthy();
  });
});
